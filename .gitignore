node_modules
dist
.vercel
.env
.env.local

# Android native project (Capacitor) — the project itself is committed, but
# build outputs, Gradle caches, and google-services.json are regenerable
# (see notifikasi_guru_tu_push memory for the `firebase apps:sdkconfig` command)
# and shouldn't sit in git even though none of them are secret.
android/app/build/
android/build/
android/.gradle/
android/app/google-services.json
android/local.properties
resources/

# JVM crash dumps left behind by crashed Gradle/Java processes
hs_err_pid*.log
replay_pid*.log
